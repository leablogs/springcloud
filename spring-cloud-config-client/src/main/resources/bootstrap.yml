server:  port: 5002spring:  application:    name: didispace # 对应配置文件的application名  cloud:    config:      discovery:        enabled: true        service-id: cloud-config-server      # uri: http://localhost:5001/      profile: dev      label: master # 分支名称      fail-fast: true # 先判断config server是否征程，并快速相应内容      retry:        multiplier: 1000        initial-interval: 1100        max-interval: 2000        max-attempts: 6  rabbitmq:    host: rabbitmq.leablogs.com    port: 5672    username: guest    password: guest    virtual-host: /management:  endpoints:    web:      exposure:        include: "*"eureka:  client:    service-url:      defaultZone: http://localhost:9001/eureka/    register-with-eureka: true    fetch-registry: true  instance:    instance-id: ${spring.application.name}:${server.port}    prefer-ip-address: true    status-page-url-path: /index.html    lease-expiration-duration-in-seconds: 2    lease-renewal-interval-in-seconds: 1